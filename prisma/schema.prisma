// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "cockroachdb"
  url      = "postgresql://josef:ZfExggCM1eJvjqG8-w_ozg@queen-satyr-5526.6wr.cockroachlabs.cloud:26257/defaultdb?sslmode=verify-full"
}

model Tab {
  id       Int                  @id @default(sequence()) 
  title String    
  notes Note[] 
}

model Note {
  id Int @id @default(sequence())  
  tab Tab @relation(fields: [tabId], references: [id])  
  tabId Int  
  body String 
}

-- CreateTable
CREATE TABLE "Tab" (
    "id" INT4 NOT NULL GENERATED BY DEFAULT AS IDENTITY,
    "Title" STRING NOT NULL,

    CONSTRAINT "Tab_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "Note" (
    "id" INT4 NOT NULL GENERATED BY DEFAULT AS IDENTITY,
    "tabId" INT4 NOT NULL,
    "body" STRING NOT NULL,

    CONSTRAINT "Note_pkey" PRIMARY KEY ("id")
);

-- AddForeignKey
ALTER TABLE "Note" ADD CONSTRAINT "Note_tabId_fkey" FOREIGN KEY ("tabId") REFERENCES "Tab"("id") ON DELETE RESTRICT ON UPDATE CASCADE;
